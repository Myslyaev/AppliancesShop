@page "/myorder"
@rendermode InteractiveServer

<PageTitle>Заказы</PageTitle>

<h1>Статус заказа : Заказ 344К готов к получению</h1>

<h2>История заказов:</h2>

<div class="card w-100 mt-2">
  <div class="card-body">
    <h6 class="card-subtitle mb-2 text-muted">24/08/2023</h6>
    <p class="card-text">"Микроволновая печь Samsung MS23K3513AK/BW"</p>
    <a href="/products" class="card-link">Смотреть в каталоге</a>
  </div>
</div>

<div class="card w-100 mt-2">
  <div class="card-body">
    <h6 class="card-subtitle mb-2 text-muted">12/11/2022</h6>
    <p class="card-text">"Пылесос Karcher VC 2"</p>
    <p class="card-text">"Холодильник Indesit ITR 5180 S"</p>
    <a href="/products" class="card-link">Смотреть в каталоге</a>
  </div>
</div>

@*<button class="btn btn-primary"  @onclick="ToggleModal">Холодильник</button>

<div class="@modalClass" tabindex="-1" style="display: @displayMode;" >
    <div class="modal-dialog" >
       <div class="modal-content" >
            <div class="modal-header" >
                <h5 class="modal-title" >Modal title</h5>
                <button class="close" @onclick="ToggleModal" aria-label="Close" >
                    <span aria-hidden="true">&times;</span>
                </button>
            </div>
            <div class="modal-body">
                @ChildContent
            </div>
            <div class="modal-footer">
                <button class="btn btn-secondary"  @onclick="ToggleModal">Close</button>
                <button class="btn btn-primary">Save changes</button>
            </div>
        </div>
    </div>
</div>*@


@*@code {

    bool open = false;
    string modalClass => open ? "modal fade show" : "modal fade";
    string displayMode => open ? "block" : "none";


    private void ToggleModal()
    {
        open = !open;       
        StateHasChanged();
    }

    [Parameter]
    public RenderFragment ChildContent { get; set; }
}*@


@*<p role="status">Current count: @currentCount</p>

<button class="btn btn-error" @onclick="IncrementCount">Click me</button>

@code {
    private int currentCount = 0;

    private void IncrementCount()
    {
        currentCount++;
    }
}*@
